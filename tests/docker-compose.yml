version: "3.7"
services:
  mealie:
    image: elestio4test/mealie:latest
    restart: always
    ports:
      - "172.17.0.1:9925:3000"
    volumes:
      - ./data:/app/data/
    environment:
      - ALLOW_SIGNUP=true
      - PUID=1000
      - PGID=1000
      - TZ=America/Anchorage